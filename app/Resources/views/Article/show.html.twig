{% extends 'base.html.twig' %}

{% block body %}
    <h1>Toutes les dernières news</h1>
    <a href="create-article">
        <button class="btn btn-success">Nouvel article</button>
    </a>
    {% for article in articles %}
        <h2>
            <a href="view-article/{{article.id}}">{{article.title}}</a>
            <a href="update-article/{{article.id}}">
                <i class="ml-4 fas fa-edit"></i>
            </a>
            <a href="delete-article/{{article.id}}">
                <i class="ml-4 fas fa-trash-alt"></i>
            </a>
        </h2>
    {% endfor %}
{% endblock %}